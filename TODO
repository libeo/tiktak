creer des tickets pour les use case desginer
can't edit task name after validation error. why ?
wtf does 'scheduled' do ? (in Task model)
add link to user controller
add function to include everyone in project permissions
