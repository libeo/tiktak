if @old_task
  page.replace_html ".#{@old_task.dom_id}", :partial => "tasks/task_row", :locals => {:task => @old_task}
  page.visual_effect :shake, ".#{@old_task.dom_id}"
end

page.replace(".#{ @current_sheet.task.dom_id }", :partial => "tasks/task_row", 
        :locals => {:task => @current_sheet.task })
page.replace_html "sheet_info", :partial => "header/sheet_info"
page.replace_html 'time_left', :partial => 'header/time_left'
page.call "UpdateDnD"
