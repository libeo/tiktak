<p><%= link_to t(:create_comment), new_page_path %></p>
<% @pages.each do |project_name, pages| %>

  <h2><%= project_name %></h2>
  <ul>
  <% pages.each do |page| %>
    <li><%= link_to page.name, page %></li>
  <% end %>
<% end %>
