<p><%= link_to t(:create_note), {:controller => 'pages', :action => 'new'} %></p><br />
<% @pages.each do |project_name, pages| %>

  <h3><%= project_name %></h3>
  <ul>
  <% pages.each do |page| %>
    <li><%= link_to page.name, {:controller => 'pages', :action => 'edit', :id => page.id} %></li>
  <% end %>
<% end %>
